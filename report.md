# [최종 과제] Dodge the Spheres: 1인칭 서바이벌 게임

**이름:** 이고은
**학번:** 2024404017
**소속:** 정보융합학부 시각기술융합전공

## 1. 게임 기획 및 개발 상세 (기획 및 완성도)

### 1.1. 게임 기획 및 컨셉
'Dodge the Spheres'는 1인칭 시점의 미니멀리스트 3D 생존 회피 게임입니다. 어두운 공간 속에서 무작위로 생성되어 플레이어를 향해 날아오는 구체(Spheres)들을 피하며 최대한 오래 살아남는 것이 목표입니다. 
- **조작 방법**: WASD 키를 이용한 평면 이동 및 마우스를 이용한 1인칭 시점(PointerLockControls) 전환.
- **게임 루프**: 'Ready' -> 'Playing' -> 'Game Over'의 3단계 페이즈로 구성되어 있으며, 생존 시간(Score)과 최고 기록(Best)이 UI로 제공됩니다.

### 1.2. 개발 기술 및 환경
- **라이브러리**: React, Three.js, TanStack Router
- **렌더링**: WebGL 기반 래스터라이저(`THREE.WebGLRenderer`) 사용, 안티앨리어싱(Antialias) 및 PCFSoftShadowMap 그림자 렌더링 적용.

* **웹 구동 링크:** [https://goeun0422.github.io/레포지토리이름/] (👈 본인 깃허브 페이지 링크 기입)

![게임 플레이 메인 화면 캡처](./images/img1.jpg)
*그림 1: 어두운 방 안에서 구체들이 날아오고 있는 역동적인 전체 플레이 대기 화면*

---

## 2. 컴퓨터그래픽스 강의 내용과 구현 내용 매핑

### 2.1. Rasterization 파이프라인 및 애니메이션
강의에서 다룬 래스터라이제이션(Rasterization)은 3D 씬의 정점(Vertices)과 프리미티브(Primitives)를 2D 화면의 픽셀(Fragments)로 변환하는 핵심 그래픽스 파이프라인입니다. 본 게임에서는 `WebGLRenderer`를 사용하여 3D 공간의 구체 기하학(Geometry) 데이터를 매 프레임 모니터 화면의 픽셀로 변환하여 출력합니다. 특히 `renderer.setPixelRatio`와 `antialias: true` 속성을 통해 픽셀 계단 현상(Jaggies)을 완화했습니다. 
또한 시간의 흐름에 따라 3D 공간상 객체에 Local Transform을 적용하는 애니메이션 기법을 활용하여, 매 프레임 구체 벡터에 Translation(이동 변환)을 가해 플레이어 방향으로 지속적으로 다가오는 동적 장애물을 구현했습니다.

### 2.2. 씬 그래프(Scene Graph) 및 조명(Lighting)
3D 씬은 계층적인 노드 구조인 씬 그래프로 구성되며, 모델 변환(Model Transform)은 이 트리를 기반으로 이루어집니다. 본 게임은 `THREE.Scene`을 루트(Root) 노드로 환경광, 구체, 프로브를 자식으로 가집니다. 
특히 1인칭 시점 몰입감을 위해 카메라(`PerspectiveCamera`) 객체에 손전등 역할을 하는 `PointLight`를 자식 노드로 추가하였습니다. 이를 통해 카메라의 Local Transform(이동 및 회전)이 변경될 때마다 조명도 부모의 변환을 상속받아 함께 이동합니다.
조명 모델 적용 시 `MeshStandardMaterial`을 사용하여 물리 기반 렌더링(PBR)을 구현했습니다. 플래시라이트의 빛이 다가오는 구체에 닿을 때 표면의 매끄러운 부분에 정반사(Specular Highlight)가 발생하며, 빛이 막히는 공간에는 그림자 맵(`shadowMap`) 기술을 적용해 동적인 그림자(Shadow)를 실시간으로 렌더링합니다.

![빛과 그림자 적용 화면 캡처](./images/img5.jpg)
*그림 2: 플래시라이트를 받아 구체 표면에 광택(Specular Highlight)이 맺혀 있고, 그 뒤로 길게 바닥에 그림자(Shadow)가 드리워지며 래스터라이즈 된 장면*

---

## 3. Global Illumination (DDGI) 기술 적용

본 과제의 핵심 기술 요건인 **GI 기술(DDGI)**을 게임 내에 구현하였습니다. 직접광(Direct Lighting)만으로는 현실적인 빛의 퍼짐을 표현하기 어려워, 공간 내에서 빛이 반사되어 들어오는 간접광(Indirect Lighting) 시뮬레이션이 필수적입니다. 본 게임은 공간을 샘플링하는 프로브 기반의 DDGI (Dynamic Diffuse Global Illumination) 기법을 적용했습니다.

* **프로브 볼륨(Probe Volume) 생성:** 무한한 빛의 반사를 근사하기 위해 공간 전체를 덮는 3D 격자 형태의 프로브를 배치했습니다. 각 프로브는 간접광을 내뿜는 `PointLight`와 시각화용 `Mesh`를 포함합니다.
* **실시간 레이캐스팅 (Raycasting):** 프루브에서 사방으로 Ray를 쏘아 플레이어와의 거리 및 반사광을 계산합니다. 단일 플래시라이트에서 발생한 빛이 벽면에 부딪힌 뒤, 빛이 직접 닿지 않는 구체의 뒷면이나 구석진 공간까지 반사되어 묻어나는 동적 난반사(Diffuse) 간접 조명 효과를 시뮬레이션 하였습니다.
* **디버그 모드 (P키 토글 구현):** 교수님께서 채점 시 GI 기술 적용 여부를 시각적으로 명확하게 확인하실 수 있도록, 게임 구동 중 `P`키를 누르면 숨겨져 있던 프로브 격자(Wireframe)가 화면에 나타나는 토글 기능을 추가했습니다.

![DDGI 프로브 시각화 화면 캡처](./images/img3.jpg)
*그림 3: P키를 눌러 활성화한 DDGI 프로브 격자 시각화 모습. 공간에 배치된 프로브들이 간접광 효과를 연산하여 암흑이 아닌 은은한 반사광을 만들어냄.*

---

## 4. 결론 및 플레이 방법

* **조작:** WASD(이동), 마우스(시야 회전)
* **시작:** 화면 클릭, Space, 또는 Enter
* **디버그:** P (DDGI 프로브 시각화 켜기/끄기)

![게임 오버 및 HUD 화면 캡처](./images/img4.jpg)
*그림 4: Game Over 시 최고 기록(Best)과 현재 생존 시간을 보여주는 UI*

